# Script Output Contract

最终输出必须使用冻结 `script/0.1` 十区：Contract Header、Script Brief、Cast & Voice、Scene Architecture、Scene Bodies、Dialogue & Voiceover Ledger、Coverage、Production Constraints、Audit、Handoff。

Header 的 `truth_lock` 指向 Source Lock，`narrative_lock` 指向 Adaptation Plan。Scene 必须含 scene_id、time_budget、location_time、characters、dramatic_function、narrative_node_ids、fact_ids、knowledge_ids、boundary_ids、entry_state、actions、dialogue_or_voiceover、exit_state。下游是 `wenjing-video-visual-bible`。

