# P3 · Script Draft

*P3 加载：script-output-contract。*

把锁定 span map 写入 `03_script_DRAFT.md`。心理和抽象叙述只能使用已批准 `OBSERVABLE_EQUIVALENT` 或原义 VO；不生成镜头语言。

输出 schema：完整 `script/0.1` 十区草稿；每个 scene/action/line 关联 source span 或 adaptation node。

Hard Stop：完成机器结构检查后进入 P4；发现无法等义可视化则留在 P3 等待修正，不得自动发明情节。

