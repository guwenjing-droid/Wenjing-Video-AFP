# Script Output Contract

使用冻结 `script/0.1` 十区。Header `truth_lock` 指向 Story Brief/Canon Lock，`narrative_lock` 指向 Story Plan。Scene 和 line 关联 canon_id（放在 fact_ids）、node_id、boundary_id；knowledge_ids 可为空。下游固定 `wenjing-video-visual-bible`。

