# wenjing-video-source-locked-adapter

SOURCE-LOCKED 路线入口。锁定指定来源范围后，忠实输出通用 `03_script_LOCKED.md`，再交现有 Visual Bible → Storyboard → Reviewer → Producer。

不生成分镜或媒体；Reference Analysis 可选且永远低于来源事实与用户约束。v0.1.0 使用 Dry Run/Mock 验收。

