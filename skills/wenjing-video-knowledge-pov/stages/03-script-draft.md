# P3 · Script Draft

*P3 加载：script-output-contract。*

把知识计划写成可拍场次、动作、逐字台词/旁白。POV 仅在锁定模式为 POV 时使用；抽象概念需明确例子或隐喻映射，不写镜头指令。

输出 schema：完整 `script/0.1` DRAFT，scene/line 关联 claim_id、knowledge_id、boundary_id、narrative node。

Hard Stop：结构机器校验通过后进 P4；无法准确解释的 claim 留在 P3，不得自动简化成错误类比。

