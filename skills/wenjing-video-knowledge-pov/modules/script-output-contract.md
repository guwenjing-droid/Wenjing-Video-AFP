# Script Output Contract

使用冻结 `script/0.1` 十区。Header `truth_lock` 指向 Knowledge Truth Lock，`narrative_lock` 指向 Knowledge Narrative Plan。Scene/line 用 fact_ids、knowledge_ids、boundary_ids、narrative_node_ids 保持来源。下游固定为 `wenjing-video-visual-bible`，不要求其识别本路线内部方法。

